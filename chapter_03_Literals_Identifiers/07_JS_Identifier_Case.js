var name = "Priya";

var firstName = "Priya";

var lastName = "Ayyanar"; // camel case

var first_name = "Priya"; // snake case

// JavaScript Identifier Cases

// camelCase
let myVariableName = "camelCase";
let firstName = "John";
let getUserData = "function name";

// PascalCase
class MyClassName { }
function MyFunctionName() { }
let PersonName = "Jane";

// snake_case
let my_variable_name = "snake_case";
let first_name = "John";
let get_user_data = "function name";

// SCREAMING_SNAKE_CASE
const MAX_VALUE = 100;
const API_KEY = "abc123";
const DATABASE_URL = "localhost";

// kebab-case
// my-variable-name (used in HTML/CSS, not valid JS identifier)
// first-name (used in HTML/CSS, not valid JS identifier)

// Train-Case
// My-Variable-Name (used in HTML/CSS, not valid JS identifier)
// First-Name (used in HTML/CSS, not valid JS identifier)

// dot.case
// my.variable.name (used in file names/configs, not valid JS identifier)
// first.name (used in file names/configs, not valid JS identifier)

// Hungarian notation
// Prefixes the variable name with a type indicator so you know the data type just by looking at the name.
// System Hungarian: prefix shows the actual data type (str, int, b, arr, obj)
// Apps Hungarian: prefix shows the logical purpose or kind of value (us for unsafe string, s for safe string)
let strName = "string prefix";        // str = string
let intAge = 30;                      // int = integer/number
let bIsActive = true;                 // b = boolean
let arrNames = ["John", "Jane"];      // arr = array
let objUser = { name: "John" };       // obj = object
let elButton = document.getElementById("btn"); // el = DOM element
