let test_results = ["Pass", "Skip", "Fail", "Pass"];

test_results.forEach(function (result, index) {
    console.log('Test ' + index + " => " + result);
});

// let testResults = ["Pass", "Fail", "Pass", "Skip"];


// testResults.forEach(function (result, index) {
//     console.log("Test " + index + "=> " + result);
// })